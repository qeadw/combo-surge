/** @type {import('next').NextConfig} */
const nextConfig = {
  // No static export - requires Node.js server
  // Run with: npm run dev (development) or npm run build && npm start (production)
};

module.exports = nextConfig;
